import styled from 'styled-components';

const DescriptionRevenger = styled.p`
  font-size: 20rem;
  color: var(--color-gray-light);
  font-weight: 400;
  display: inline-block;
`;
export default DescriptionRevenger;
